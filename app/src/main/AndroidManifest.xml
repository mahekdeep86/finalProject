<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".news.NewsFeedActivity"
            />
        <activity android:name=".articles.ArticleSearchActivity" />
        <activity android:name=".news.NewsArticleViewActivity" />
        <activity android:name=".dictionary.DictionaryActivity"
            android:parentActivityName=".MainActivity"
            />
        <activity android:name=".flights.FlightActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".articles.ArticleExpandViewActivity" />
        <activity android:name=".articles.ArticleViewActivity" />
        <activity android:name=".news.NewsFeedViewActivity" />
        <activity android:name=".flights.FlightMoreDetailActivity"
            android:parentActivityName=".flights.FlightActivity"/>
    </application>

</manifest>